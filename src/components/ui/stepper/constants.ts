/** Circle wrap — keep fixed so step columns never shift. */
export const STEP_NODE_SIZE = 44
/** Half of solid circle (32). Connectors end at this radius for a flush join. */
export const STEP_CIRCLE_RADIUS = 16
export const STEP_TRACK_HEIGHT = 4
export const PROGRESS_MS = 280
